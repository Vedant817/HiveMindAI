[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 140
extend-exclude = ["symphony", "workspace", "local_state"]

[tool.ruff.lint]
select = ["F"]
ignore = ["E501"]
